meta {
  name: Deletar Sessão
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/atividades/{{atividadeId}}/sessoes/{{sessaoId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

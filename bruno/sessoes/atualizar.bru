meta {
  name: Atualizar Sessão
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/atividades/{{atividadeId}}/sessoes/{{sessaoId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "duracaoSegundos": 1800,
    "observacoes": "Sessão atualizada"
  }
}
